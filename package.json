{
  "name": "minimmizer",
  "version": "1.1.1",
  "description": "A lightweight set of CSS utilities that you can use in every web project",
  "scripts": {
    "build": "node-sass src/minimmizer.scss dist/minimmizer.css --output-style expanded --source-map true && node-sass src/minimmizer.scss dist/minimmizer.min.css --output-style compressed --source-map true"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lumennn/minimmizer.git"
  },
  "author": "lumennn",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/lumennn/minimmizer/issues"
  },
  "homepage": "https://github.com/lumennn/minimmizer#readme",
  "devDependencies": {
    "node-sass": "^7.0.0"
  }
}
